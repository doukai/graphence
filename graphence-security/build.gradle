plugins {
    id 'java-library'
    id 'maven-publish'
}

group 'org.graphoenix'
version '0.1.2'

repositories {
    mavenCentral()
    jcenter()
}

java {
    withJavadocJar()
    withSourcesJar()
}

dependencies {
    implementation project(':graphence-core')
    implementation 'org.graphoenix:graphoenix-core:0.1.2'
    implementation 'org.graphoenix:graphoenix-http-server:0.1.3'
    implementation 'org.graphoenix:graphoenix-structure:0.1.2'
    implementation 'org.graphoenix:nozdormu-inject:0.1.0'

    annotationProcessor project(':graphence-core')
    annotationProcessor 'org.graphoenix:graphoenix-core:0.1.2'
    annotationProcessor 'org.graphoenix:graphoenix-http-server:0.1.3'
    annotationProcessor 'org.graphoenix:graphoenix-structure:0.1.2'
    annotationProcessor 'org.graphoenix:nozdormu-inject:0.1.0'

    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.9.2'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.9.2'
}

test {
    useJUnitPlatform()
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'org.graphoenix'
            artifactId = 'graphence-security'

            from components.java

            pom {
                name = 'graphence-security'
                description = 'graphence-security'
                url = 'https://github.com/doukai/graphence'
                inceptionYear = '2023'
                licenses {
                    license {
                        name = 'MIT'
                        url = 'https://spdx.org/licenses/MIT.html'
                    }
                }
                developers {
                    developer {
                        id = 'doukai'
                        name = 'Dou Kai'
                    }
                }
                scm {
                    url = 'https://github.com/doukai/graphence'
                    connection = 'scm:git://github.com:doukai/graphence.git'
                    developerConnection = 'scm:git://github.com:doukai/graphence.git'
                }
            }
        }
    }

    repositories {
        maven {
            url = layout.buildDirectory.dir('staging-deploy')
        }
    }
}

tasks.build.doFirst {
    mkdir "$buildDir/jreleaser"
}

jreleaser {
    gitRootSearch = true
    signing {
        active = 'ALWAYS'
        armored = true
    }
    deploy {
        maven {
            mavenCentral {
                sonatype {
                    active = 'ALWAYS'
                    url = 'https://central.sonatype.com/api/v1/publisher'
                    stagingRepository('build/staging-deploy')
                }
            }
        }
    }
}